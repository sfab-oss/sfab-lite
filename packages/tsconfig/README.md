# `@sfab-lite/tsconfig`

Shared TypeScript configs for the sfab-lite monorepo (same shape as
`sfab-starter`'s `@workspace/tsconfig`).

- `base.json` — default NodeNext / strict
- `react-library.json` — React libraries
- `tanstack-start.json` — TanStack Start apps (factory / template)
