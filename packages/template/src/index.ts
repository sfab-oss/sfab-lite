/**
 * @sfab-lite/template — sfab-starter-lite seed payload; independently runnable; kernel-closure source
 * Scaffold only (S0). Real implementation lands in later stages.
 */
export const packageName = "@sfab-lite/template" as const;
