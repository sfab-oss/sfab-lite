/**
 * @sfab-lite/kernel — frozen universe: pinned deps, prebuild pipeline (types VFS + client chunks)
 * Scaffold only (S0). Real implementation lands in later stages.
 */
export const packageName = "@sfab-lite/kernel" as const;
