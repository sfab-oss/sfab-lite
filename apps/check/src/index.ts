/**
 * @sfab-lite/check — pruned TypeScript check worker (plain, async, publish gate)
 * Scaffold only (S0). Real implementation lands in later stages.
 */
export const packageName = "@sfab-lite/check" as const;
