/**
 * @sfab-lite/factory — host worker + factory UI (admin API, serve, publish)
 * Scaffold only (S0). Real implementation lands in later stages.
 */
export const packageName = "@sfab-lite/factory" as const;
