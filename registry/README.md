# registry/

Future recipes repo: versioned content on its own cadence, a consumer of
`framework/`, not a part of it.

Empty in this PR — the package, schema, and hosted `add` land with the
registry milestone. Present now so the top-level future-repo map is
visible and the direction gate already names this zone.
