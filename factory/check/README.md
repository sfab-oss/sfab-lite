# `@sfab-lite/check`

HTTP shell for the check worker: admin token, `GET /health`, `POST /check`.
The units runner lives in `@sfab-lite/verbs/check`. Measurement scripts and
the throwaway `src/exp/` worker stay here.
