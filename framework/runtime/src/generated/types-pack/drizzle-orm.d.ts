/** AUTO-GENERATED by framework/runtime/scripts/gen-drizzle-surface.mjs — do not edit */

// Cheap sqlite/D1 surface. Real signatures cannot be copied — see
// framework/runtime/scripts/drizzle-seams.mjs and
// docs/notes/2026-08-14-generator-spike.md.
// Support: Column: Real column types are dialect builder graphs (SQLiteTextBuilderInitial, SQLiteIntegerBuilderInitial). Query: Real query builders are dialect-generic classes that pull column-builder. Database: Real drizzle() from d1 is DrizzleD1Database extending BaseSQLiteDatabase. RowOf: Cheap mapped row type; the real row graph rides the dialect tables. InsertValues: Allows V | SQL so .set({ totalCents: sql`…` }) typechecks; real insert types are dialect-specific. SQL: Brand-only stand-in; the real SQL class pulls the sql-js graph.
// db.query.*.findFirst returns Promise<any> — not the long-term relational API; unblocks row.id in session-context. A typed Record<string, unknown> made row.id unusable.

export interface SQL { readonly __sqlBrand: true; }
export interface Column<T = unknown> {
  readonly __columnType: T;
  primaryKey(): Column<Exclude<T, null>>;
  notNull(): Column<Exclude<T, null>>;
  unique(): Column<T>;
  default(value: T | SQL): Column<T>;
  $onUpdate(fn: () => T): Column<T>;
  references(fn: () => Column<unknown>, opts?: { onDelete?: string }): Column<T>;
}

export type RowOf<T> = {
  [K in keyof T]: T[K] extends Column<infer V> ? V : never;
};

export interface Query<Row> {
  from<T extends Record<string, Column<unknown>>>(
    table: T
  ): Query<Row extends Record<string, never> ? RowOf<T> : Row>;
  where(clause: SQL): Query<Row>;
  orderBy(...clauses: SQL[]): Query<Row>;
  limit(n: number): Query<Row>;
  then<TResult1 = Row[], TResult2 = never>(
    onfulfilled?: ((value: Row[]) => TResult1 | PromiseLike<TResult1>) | null,
    onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
  ): Promise<TResult1 | TResult2>;
}

type InsertValues<T> = {
  [K in keyof T]?: T[K] extends Column<infer V> ? V | SQL : never;
};

export interface Database {
  select(): Query<Record<string, never>>;
  select<S extends Record<string, Column<unknown>>>(
    shape: S
  ): Query<RowOf<S>>;
  insert<T extends Record<string, Column<unknown>>>(table: T): {
    values(row: InsertValues<T> | Array<InsertValues<T>>): {
      onConflictDoNothing(): {
        returning(shape?: Record<string, Column<unknown>>): Promise<
          Array<RowOf<T>>
        >;
      };
      returning(shape?: Record<string, Column<unknown>>): Promise<
        Array<RowOf<T>>
      >;
    };
  };
  update<T extends Record<string, Column<unknown>>>(table: T): {
    set(row: InsertValues<T>): {
      where(clause: SQL): {
        returning(shape?: Record<string, Column<unknown>>): Promise<
          Array<RowOf<T>>
        >;
      };
    };
  };
  delete<T extends Record<string, Column<unknown>>>(table: T): {
    where(clause: SQL): {
      returning(shape?: Record<string, Column<unknown>>): Promise<
        Array<RowOf<T>>
      >;
    };
  };
  query: Record<
    string,
    {
      findFirst: (opts?: { where?: SQL }) => Promise<any>;
    }
  >;
}

// and: Real and() is typed over SQLWrapper combinators that drag the sql-js graph.
export declare function and(...clauses: SQL[]): SQL;

// asc: Real asc() takes AnyColumn / SQLWrapper; copying pulls column-builder.
export declare function asc(column: Column<unknown>): SQL;

// count: Real count() takes an optional wrapper; a zero-arg-only seam missed documents.
export declare function count(expression?: Column<unknown> | SQL): Column<number>;

// desc: Same as asc: real signature is dialect-generic over AnyColumn.
export declare function desc(column: Column<unknown>): SQL;

// drizzle: Real drizzle() from d1 returns DrizzleD1Database; copying re-opens BaseSQLiteDatabase.
export declare function drizzle(
  db: D1Database,
  opts?: { schema?: object }
): Database;

// eq: Real eq is BinaryOperator; copying that alias re-opens the dialect graphs.
export declare function eq<T>(left: Column<T>, right: T | null): SQL;

// index: Real index() returns a dialect ExtraConfigBuilder.
export declare function index(name: string): {
  on(...columns: Column<unknown>[]): unknown;
};

// integer: Real integer() returns SQLiteIntegerBuilderInitial; timestamp_ms / boolean overloads are sqlite-only seams.
export declare function integer(name: string): Column<number | null>;
export declare function integer(
  name: string,
  opts: { mode: "timestamp_ms" }
): Column<Date | null>;
export declare function integer(
  name: string,
  opts: { mode: "boolean" }
): Column<boolean | null>;

// notExists: Real notExists() takes a subquery wrapper type from the sql graph.
export declare function notExists(query: unknown): SQL;

// relations: Real relations() is a generic over table config that pulls the relations graph.
export declare function relations(
  table: object,
  fn: (helpers: {
    one: (...args: unknown[]) => unknown;
    many: (...args: unknown[]) => unknown;
  }) => unknown
): unknown;

// sql: Real sql tagged template is the SQL class; copying pulls sql-js + dialect helpers.
export declare function sql(
  strings: TemplateStringsArray,
  ...values: unknown[]
): SQL;

// sqliteTable: Real sqliteTable is SQLiteTableFn; copying that alias re-opens sqlite-core builders.
export declare function sqliteTable<Cols extends Record<string, Column<unknown>>>(
  name: string,
  columns: Cols,
  extra?: (table: Cols) => unknown[]
): Cols;

// text: Real text() returns SQLiteTextBuilderInitial. The enum overload keeps T[number] so client InferResponseType (EntityKind) stays "customer" | "vendor" instead of string — opts?: object alone failed seed typecheck on entities.tsx.
export declare function text<const T extends readonly string[]>(
  name: string,
  opts: { enum: T }
): Column<T[number] | null>;
export declare function text(name: string, opts?: object): Column<string | null>;

// uniqueIndex: Same as index: real uniqueIndex() is a dialect ExtraConfigBuilder.
export declare function uniqueIndex(name: string): {
  on(...columns: Column<unknown>[]): unknown;
};
